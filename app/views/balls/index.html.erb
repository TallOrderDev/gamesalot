<p id="notice"><%= notice %></p>

<h1>Balls</h1>

<table>
  <thead>
    <tr>
      <th>Number</th>
      <th>Weight</th>
      <th>Scale game</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @balls.each do |ball| %>
      <tr>
        <td><%= ball.number %></td>
        <td><%= ball.weight %></td>
        <td><%= ball.scale_game %></td>
        <td><%= link_to 'Show', ball %></td>
        <td><%= link_to 'Edit', edit_ball_path(ball) %></td>
        <td><%= link_to 'Destroy', ball, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Ball', new_ball_path %>
