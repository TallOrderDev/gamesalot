<%= form_for(twentyfortyeight_game) do |f| %>
  <% if twentyfortyeight_game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(twentyfortyeight_game.errors.count, "error") %> prohibited this twentyfortyeight_game from being saved:</h2>

      <ul>
      <% twentyfortyeight_game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :moves %>
    <%= f.number_field :moves %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
